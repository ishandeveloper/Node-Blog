<%- include('header'); -%>
    <div class="animated fadeIn row" style="width:105vw;animation-duration: 1.5s;">
        <div class="col">
            <% for(var i=0;i<posts.length;i++){ %>
            <div class="jumbotron jumbotron-fluid">
                <div class="container">
                  <h1 class="display-4"><%= title[i] %></h1>
                  <p class="lead"><%= posts[i] %></p>
                </div>
              </div> 
            <% } %>
        </div>
    </div>
<%- include('footer'); -%>